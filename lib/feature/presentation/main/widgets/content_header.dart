import 'package:flutter/material.dart';

import 'decoration_section.dart';

class ContentHeader extends StatelessWidget {
  const ContentHeader({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 64,
      // color: const Color(0xFF36DF5B),
    );
  }
}
